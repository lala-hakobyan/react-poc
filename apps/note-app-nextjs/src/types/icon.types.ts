export type IconConfig = {
  iconName: string;
  className?: string;
  size?: IconSize;
  color?: IconColor;
}

export type IconSize = 'sm' | 'md' | 'lg';

export type IconColor = 'accent' | 'secondary' | 'primary';
