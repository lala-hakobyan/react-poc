# new-feature-workflow

In "apps/note-app-nextjs", add a new feature: a new route named "settings".
Add page to header as a link. It should have the following data:
Page Title: Settings    
Form Fields:
- First Name (text input)
- Last Name (text input)
- Email (email input)
- Items Per Page (number input)
- Enable Email Sync (checkbox)
- Buttons: Save, Cancel
Action: For now, just log all form values to the console when clicking Save.